defmodule FlashProfile.Clustering.Linkage do
  @moduledoc """
  Linkage methods for hierarchical clustering.

  Implements different linkage strategies (single, complete, average, etc.)
  that determine how distances between clusters are computed during
  hierarchical clustering.
  """
end
